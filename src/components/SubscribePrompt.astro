<aside class="subscribe-prompt" aria-label="邮件订阅提醒">
  <span aria-hidden="true">✉</span>
  <p>
    如果你喜欢本站，也可以
    <a href="/about/#email-subscribe">邮件订阅提醒</a>。
  </p>
</aside>

<style>
  .subscribe-prompt {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    margin: 28px 0 18px;
    border: 1px solid color-mix(in srgb, var(--color-gold) 48%, var(--color-border));
    border-radius: var(--radius-md);
    background: color-mix(in srgb, var(--color-gold) 8%, var(--color-card-solid));
    padding: 14px 18px;
    text-align: center;
  }

  .subscribe-prompt span {
    flex: 0 0 auto;
    color: var(--color-gold);
    font-size: 18px;
  }

  .subscribe-prompt p {
    margin: 0;
    color: var(--color-text-2);
  }

  .subscribe-prompt a {
    color: var(--color-gold);
    font-weight: 700;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 4px;
  }

  .subscribe-prompt a:hover {
    color: var(--color-text-1);
  }

  @media (max-width: 520px) {
    .subscribe-prompt {
      align-items: flex-start;
      justify-content: flex-start;
      text-align: left;
    }
  }
</style>
